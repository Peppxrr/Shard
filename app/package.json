{
  "name": "shard",
  "productName": "Shard",
  "author": "Peppxrr",
  "version": "0.1.3",
  "description": "Medal-style game clipper — RAM replay buffer, hotkeys, library, editor, Discord-size exports.",
  "license": "GPL-2.0",
  "private": true,
  "main": "dist/main/main/main.js",
  "scripts": {
    "verify": "node ../scripts/verify.mjs",
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build && tsc -p tsconfig.main.json",
    "test:previews": "node --no-warnings --experimental-strip-types scripts/test-timeline-previews.mjs",
    "test:editor": "node --no-warnings --experimental-strip-types scripts/test-editor.mjs",
    "postinstall": "node scripts/postinstall.mjs",
    "package": "npm run build && node scripts/release.mjs validate && node scripts/release.mjs notes && node scripts/verify-core-bin.mjs && electron-builder --win --publish never",
    "test:updater": "node --no-warnings --experimental-strip-types scripts/test-updater.mjs",
    "verify:release": "node scripts/release.mjs artifacts"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "electron-updater": "6.8.9",
    "uiohook-napi": "^1.5.5"
  },
  "devDependencies": {
    "@electron/asar": "3.4.1",
    "@electron/rebuild": "^4.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.5",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "js-yaml": "4.3.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^5.9.0",
    "vite": "^8.2.1"
  }
}
