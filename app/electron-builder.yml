# electron-builder packaging. Windows: nsis per-user + portable.
# Linux targets can be added later without touching app code (core CMake is
# already cross-platform via obs-studio).
appId: com.shard.app
productName: Shard
buildVersion: 0.1.2.1
directories:
  output: release
  buildResources: build
files:
  - dist/main/**/*
  - dist/renderer/**/*
  - package.json
extraResources:
  - from: ../NOTICE
    to: NOTICE
  - from: ../LICENSE
    to: LICENSE
  - from: resources/licenses
    to: licenses
  - from: resources/core-bin
    to: core-bin
    filter:
      - "**/*"
  - from: build/icon-256.png
    to: icon.png
  - from: resources/clip-sound.wav
    to: clip-sound.wav
  - from: resources/games.json
    to: games.json
asar: true
win:
  icon: build/icon.ico
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
nsis:
  oneClick: false
  artifactName: ${productName}-Setup-${buildVersion}.${ext}
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: always
  shortcutName: Shard
portable:
  artifactName: ${productName}-${buildVersion}-portable.${ext}
